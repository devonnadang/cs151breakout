package breakout.controller;

/**
 * Message sent to signal the end of the game
 */
public class EndGameMessage implements Message {
    // Don't know what to put in here because nothing is needed to end the game.
}
